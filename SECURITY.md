# Security Policy

This repository should contain only public worker code.

Do not commit:

- `.env` files
- RunPod API keys
- OpenAI API keys
- Hugging Face write tokens
- private datasets
- private product prompts, formalizations, or student data

Configure runtime secrets only inside RunPod endpoint settings.
